# External Test Project Dependencies
# 
# Основные зависимости для тестирования TrustChain как внешней библиотеки
#
# Author: Ed Cherednik (edcherednik@gmail.com)

# TrustChain будет установлена через pip install -e ../
# поэтому здесь не указываем её как зависимость

# Testing
pytest>=7.0.0
pytest-asyncio>=0.23.0

# Optional: для расширенных тестов
requests>=2.28.0  # если нужно тестировать HTTP интеграции
aiohttp>=3.8.0    # для async HTTP клиентов

# Development (опционально)
black>=23.0.0     # форматирование кода
isort>=5.0.0      # сортировка импортов
mypy>=1.0.0       # проверка типов 